from lycee import*

x = demande("Nombre ?")

import numpy as np
import matplotlib.pyplot as plt 

np = np.linspace(0, 2*np.pi, 30)

y = np.sin(x)

plt.plot(x, y, "go-")

plt.xlabel("Angle x (rad)")
plt.ylabel("Sinus(x)")
plt.title("Fonction y=sin(x)")
plt.show(x)
